{
  "name": "hs-plugin-garmin",
  "version": "1.1.0",
  "description": "Garmin Connect health dashboard for Home Screens — daily summary, Body Battery, sleep, and recent activities",
  "license": "MIT",
  "scripts": {
    "dev": "vite build && (vite build --watch & npx -y serve@latest . -l 5173 --cors --no-clipboard)",
    "build": "vite build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.9"
  }
}
